// Import the Loader CSS file
import './Loader.css';

// Functional component for the Loader
const Loader = () => {
  // Render a span element with a "loader" class for styling
  return (
    <span className="loader"></span>
  );
}

// Export the Loader component
export default Loader;
